using ME.BECS;
using ME.BECS.Views;

public class Malytix : EntityView
{
  protected override void OnInitialize(in EntRO ent)
  {
    base.OnInitialize(in ent);
  }

  protected override void ApplyState(in EntRO ent)
  {
    base.ApplyState(in ent);
  }
}
